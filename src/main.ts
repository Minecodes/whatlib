import isbool from "./if/isbool"
import isfunc from "./if/isfunc"
import isnum from "./if/isnum"
import isobj from "./if/isobj"
import isstr from "./if/isstr"
import issymb from "./if/issymb"

export default {
    isbool: isbool,
    isfunc: isfunc,
    isnum: isnum,
    isobj: isobj,
    isstr: isstr,
    issymb: issymb
}